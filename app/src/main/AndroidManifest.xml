<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderingApp"
        tools:targetApi="31">



        <activity
            android:name=".Activity.OrderSummaryActivity"
            android:exported="false" />

        <activity
            android:name=".Activity.OrderingListActivity"
            android:exported="false" />

        <activity
            android:name=".Activity.DetailActivity"
            android:exported="false" /> <!-- SplashActivity is the launcher now -->

        <activity
            android:name=".Activity.SplashActivity"
            android:exported="true"> <!-- exported should be true -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity is normal activity -->

        <activity
            android:name=".Activity.MainActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".RegisterActivity" -->
        <!-- android:exported="true"> -->
        <!-- &lt;!&ndash; Optional: Define the label or theme for this activity &ndash;&gt; -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity android:name=".Activity.RegisterActivity" />

        <activity android:name=".Activity.ForgotPasswordActivity" />

        <activity android:name=".Activity.LoginActivity">

            <!-- You can add more configurations here if needed -->
        </activity>
    </application>

</manifest>